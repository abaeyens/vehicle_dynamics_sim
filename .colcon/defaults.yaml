{
    "build": {
        "cmake-args": ["-DCMAKE_BUILD_TYPE=RelWithDebInfo"],
        "merge-install": true,
        "symlink-install": true,
    },
    "test": {
        "merge-install": true,
        "event-handlers": ["console_direct+"],
    }
}

